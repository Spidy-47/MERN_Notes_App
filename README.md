# MERN Notes App (Expanded ZIP)

This package contains a full MERN notes application with JWT authentication and a React + Tailwind frontend.

## Run backend
cd backend
npm install
# create .env (see backend/.env)
npm run dev

## Run frontend
cd frontend
npm install
npm run dev

